{% extends "base.html.twig" %}

{% block header %}
    {{ parent() }}
{% endblock %}


{% block body %}

    <center>
        <h3>  Ajouter un produit </h3>
            <hr>
             <form action="{{ path('update',{'id': produit.id}) }}" method="POST">
                {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
                {{ form_widget(form) }}
                {{ form_errors(form) }}
                {{ form_rest(form) }}
                {{ form_end(form) }}
             </form>
    </center>

{% endblock %}

{% block footer %}
        {{ parent() }}
{% endblock %}